server:
  port: 8088
spring:
  application:
    name: interview-problem-3
  datasource:
    url: jdbc:h2:file:./data/testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.H2Dialect
    show-sql: true
    ddl-auto: create

